<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Каталог предметов - Учебный Портал</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">Учебный Портал</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Главная</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/catalog">Каталог предметов</a>
                    </li>
                    <% if (user && user.role === 'admin') { %>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="adminDropdown" role="button" data-bs-toggle="dropdown">
                            Админ-панель
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="/add-subject">Добавить предмет</a></li>
                            <li><a class="dropdown-item" href="/add-department">Добавить кафедру</a></li>
                            <li><a class="dropdown-item" href="/add-field">Добавить направление</a></li>
                            <li><a class="dropdown-item" href="/add-teacher">Добавить преподавателя</a></li>
                        </ul>
                    </li>
                    <% } %>
                </ul>
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link position-relative" href="/cart">
                            Корзина
                            <% if (cartCount > 0) { %>
                            <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                                <%= cartCount %>
                            </span>
                            <% } %>
                        </a>
                    </li>
                    <% if (user) { %>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown">
                            <%= user.username %> (<%= user.role %>)
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="/logout">Выйти</a></li>
                        </ul>
                    </li>
                    <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Войти</a>
                    </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>Каталог предметов</h1>
            <div class="text-muted">
                Найдено предметов: <strong><%= subjects.length %></strong>
            </div>
        </div>

        <div class="row">
            <% subjects.forEach(subject => { %>
            <div class="col-md-6 mb-4">
                <div class="card h-100">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-start mb-2">
                            <h5 class="card-title"><%= subject.name %></h5>
                            <span class="badge bg-<%= subject.type === 'лекция' ? 'primary' : subject.type === 'практика' ? 'success' : subject.type === 'семинар' ? 'warning' : 'info' %>">
                                <%= subject.type %>
                            </span>
                        </div>
                        
                        <p class="card-text">
                            <strong>Направление:</strong> <%= subject.Field.name %><br>
                            <strong>Кафедра:</strong> <%= subject.Department.name %> (<%= subject.Department.faculty %>)<br>
                            <strong>Преподаватель:</strong> <%= subject.Teacher.name %> - <%= subject.Teacher.position %><br>
                            <strong>Академических часов:</strong> <%= subject.hours %>
                        </p>
                        
                        <% if (subject.Field.description) { %>
                        <p class="card-text"><small class="text-muted"><%= subject.Field.description %></small></p>
                        <% } %>
                    </div>
                    
                    <div class="card-footer">
                        <div class="d-flex justify-content-between align-items-center">
                            <span class="h5 text-primary mb-0"><%= subject.price.toLocaleString() %> ₽</span>
                            <button class="btn btn-primary add-to-cart" 
                                    data-subject-id="<%= subject.id %>">
                                Добавить в корзину
                            </button>
                        </div>
                    </div>
                </div>
            </div>
            <% }); %>
        </div>

        <% if (subjects.length === 0) { %>
        <div class="text-center py-5">
            <h3>Нет доступных предметов</h3>
            <p class="text-muted">Попробуйте зайти позже или обратитесь к администратору</p>
        </div>
        <% } %>
    </div>

    <footer class="bg-dark text-light mt-5 py-4">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <h5>Учебный Портал</h5>
                    <p>Система выбора учебных предметов и курсов</p>
                </div>
                <div class="col-md-6 text-end">
                    <p>&copy; 2024 Учебный Портал. Все права защищены.</p>
                </div>
            </div>
        </div>
    </footer>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/cart.js"></script>
</body>
</html>